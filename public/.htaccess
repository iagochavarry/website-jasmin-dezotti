# Redirecionamento HTTPS (apenas se necessário no servidor)
# Descomente as linhas abaixo apenas se o servidor não forçar HTTPS automaticamente

# RewriteEngine On
# RewriteCond %{HTTPS} !=on
# RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Redirecionamento www para não-www (apenas se necessário)
# RewriteCond %{HTTP_HOST} ^www\.jasmindezotti\.com [NC]
# RewriteRule ^(.*)$ https://jasmindezotti.com/$1 [L,R=301]

# Headers de segurança
<IfModule mod_headers.c>
    Header always set X-Frame-Options "DENY"
    Header always set X-Content-Type-Options "nosniff"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>
